# GitHub 数据同步配置
# GitHub Personal Access Token (需要 repo 权限)
GITHUB_SYNC_TOKEN=ghp_your_github_personal_access_token_here

# GitHub 仓库 URL (格式: https://github.com/owner/repo 或 owner/repo)
GITHUB_SYNC_REPO=https://github.com/username/repository

# 同步间隔（秒），默认 300 秒（5分钟）
GITHUB_SYNC_INTERVAL=300

# ==================== AI 对话内容记录配置 ====================
# 是否启用内容记录功能（记录用户请求和 AI 响应的完整内容）
# 注意：启用此功能会增加数据库存储量
# 默认开启，如不需要可设置为 false
CONTENT_LOGGING_ENABLED=true

# 内容保留天数（自动删除 N 天前的对话记录）
# 默认 7 天，设置为 0 则永久保留
CONTENT_RETENTION_DAYS=7

# 最大记录内容长度（字符数）
# 默认 10000 字符，设置为 0 则不限制
# 超过此长度的内容将被截断
MAX_CONTENT_LENGTH=10000
